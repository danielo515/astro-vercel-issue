---
import CountdownComponent from "./Countdown.svelte";
interface Props {
  fullWidth?: boolean;
}
const { fullWidth = false } = Astro.props;
---

<CountdownComponent
  targetDate={new Date("2024-06-01T17:00:00")}
  client:load
  fullWidth={fullWidth}
/>
